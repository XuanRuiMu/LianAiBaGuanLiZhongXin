# 恋爱吧数据中心 · 优化计划

> 执行者：AI Agent。本文件只给「目的」和「要干什么」。直接开工，不要做文件外的调研或论证。

---

## 1. 目的

把本项目从「AI 问答 + 数据大屏」改造为 **一套可交付、可运维、可测试、文档齐全的 AI 数据平台**。

候选人画像：普通三本计算机本科应届。**核心目标是拿到面试**——项目先要具备实施/测试/运维/Java/BI 这些大批量岗位要求的基本面，AI 能力是差异化加分项，不是全部。

---

## 2. 目标岗位与本项目须补的能力

已按「三本本科应届可投」筛选（JD 学历门槛为「大专及以上」或「本科不卡院校」，经验 ≤1 年或明确收应届）。

| 岗位（按命中率×需求量排序） | 本项目须具备 | 期 |
| --- | --- | --- |
| **软件实施工程师**（量最大、门槛最低） | 一键部署脚本（Linux + Windows）· 多环境配置 · 健康检查 · 日志规范 · 备份恢复 · 部署与运维手册 | P0 |
| **软件测试 / 功能测试** | pytest · Spring Boot Test · E2E · 测试用例文档 · 覆盖率门禁 | P1 |
| **Java 开发**（外包 / 银行科技 / 制造业信息化） | Spring Boot + MyBatis + MySQL + Redis + **复杂 SQL 能力** | P2 |
| **BI 报表 / 数据开发** | 复杂 SQL（多表关联 / 窗口函数）· 数仓分层 · 报表导出 · 数据质量校验 | P2 |
| **AI 应用 / Agent 开发**（差异化） | MCP · LangGraph · RAG · 记忆 · 评测 | P3–P5 |
| **低代码实施 / 工作流自动化**（捡漏） | 可视化流程编排 · n8n 自定义节点 | P4 |
| **售前售后技术支持 / 技术文档** | 完整文档体系（架构 / API / 部署 / 用户手册 / FAQ / 故障排查） | P6 |
| **前端 / 小程序**（次主投） | React + ECharts；（可选）uni-app 小程序端 | P6 |

**已排除**（JD 原文卡死，不做无用功）：大厂 ERP 校招（要求双一流）、AI Agent 开发实施（要求 2–3 年）、大厂售前（要求 211/985）、AI 算法研究员（硕博起）。

---

## 3. 现状（代码事实，据此改，不要重写）

### 3.1 已有、可复用、不要动

| 服务 | 技术 | 已有能力 |
| --- | --- | --- |
| ai-service | FastAPI / LangGraph / Chroma（venv 为 **Python 3.12.13**，偏旧） | 4 节点图 `plan→execute_tools→reflect→generate`；6 个 `@tool`；RAG；SSE 流式 |
| backend-java | Maven · Spring Boot **3.5.16** · JDK **21** · MyBatis-Plus · Spring Kafka | Controller-Service-Mapper 分层；Redis 缓存（`cache/缓存服务`：Lua 分布式锁 + 空值标记 + 抖动 TTL + 单飞）；Kafka 生产消费 + 重试 + 死信；JWT + 内部令牌双鉴权；`@Scheduled` |
| analytics-frontend | React **18.3.1** / Vite 5.4.21 / TS 5.9.3 / ECharts **5.6.0** | 登录 + 大屏 + 助手三页；SSE 解析；路由守卫 |
| tts-service | FastAPI 0.115.0 / edge-tts 7.2.8 | EdgeTTS 主路 + 离线正弦兜底 + LRU 单飞；有 Dockerfile + 测试 |

### 3.2 缺失或未接线（这就是要干的活）

| 缺口 | 位置 / 说明 |
| --- | --- |
| 不可交付 | `infra/docker-compose.yml` **只有中间件**，三个业务服务都不在里面；无部署脚本、无运维手册、无备份恢复 |
| 不可测试 | 四个子服务共 15 个测试文件，**零 CI**，无测试用例文档，无覆盖率门禁 |
| MCP 未接线 | `ai-service/app/mcp_server.py` 写了 5 个工具，`main.py` **未 import 未挂载** |
| 无记忆 | 无会话存储，历史靠前端回传 20 轮；`compile()` 无 checkpointer |
| 无可观测 / 评测 / 护栏 | 声明了 `langgraph-checkpoint`、`langsmith`、`opentelemetry-*` 依赖，**零行对应代码** |
| 死信无消费者 | `love.sync.events.DLT` 只生产不消费 |
| 流式端点裸奔 | `/api/chat/stream` 无鉴权无限流，且可经工具回查内部统计 |
| 无编排能力 | 无可视化编排、无流程 DSL、无 DAG 执行引擎 |
| 无集成能力 | 无 OpenAPI 开放接口、无 Webhook、无企业渠道（飞书/企微/钉钉/邮件） |
| 无报表能力 | 只有 ECharts 大屏，无 Excel/PDF 导出、无数仓分层、无数据质量校验 |
| 标识符未收口 | `tts-service` 的 `provider.py` / `voices.py` 残留 `code`/`message`/`retryable`/`rate`/`pitch` 等英文业务标识符 |

---

## 4. 技术栈边界（去重，强制）

候选人另有三个项目。本项目**只展示它们没有的技术**，重复的一律不进简历技术栈清单。

### 4.1 已存在于其他项目 —— 本项目只做实现，不作亮点、不写进技术栈

| 技术 | 已有于 |
| --- | --- |
| React / Vite / TypeScript / Tailwind / zustand / three.js / PWA / 前端性能工程 | 个人简历 |
| Vue / Express / Node.js / Socket.IO / JWT / bcrypt / 限流 | 和我恋爱吧 |
| Docker / docker-compose | 和我恋爱吧 |
| **OpenTelemetry + Prometheus + Grafana 全家桶** | 和我恋爱吧 |
| Playwright / Vitest | 和我恋爱吧 + 个人简历 |
| JUnit / Mockito | XRM 插件 |
| Guice DI / HikariCP / 原生 JDBC | XRM 插件 |
| DeepSeek API 调用 / 基础 RAG | 和我恋爱吧 + 个人简历 |

> ⚠️ **可观测实现方式必须区分**：本项目不得再搭 OTel + Prometheus + Grafana 那一套（已展示过）。改由 **Python 侧自研轻量 Trace/Span，落 Postgres**——实现方式不同，属本项目独有能力。

### 4.2 本项目独有 —— 重点展示

Python 3.14 + FastAPI + uvicorn + pytest · LangGraph + langchain-core · **MCP / FastMCP** · **pgvector + tsvector 混合检索 + RRF + CrossEncoder Rerank** · **Spring Boot 4 + Spring AI + MyBatis-Plus + Spring Kafka** · **Redis 高级模式**（Lua 分布式锁 / 单飞 / 空值标记 / 抖动 TTL）· **Apache Kafka**（生产消费 + 重试 + 死信）· **ECharts 数据大屏** · **edge-tts 语音合成** · **@xyflow/react + 自研 DAG 执行引擎** · **n8n 自定义节点** · **数仓分层 + 数据质量** · **Maven 构建**（XRM 用 Gradle）· **报表导出**（openpyxl / reportlab）· **自研轻量 Trace**

---

## 5. 要干什么

按 P0→P6 顺序推进，每期独立可验证。**每期开工前先跑通上一期。**

### P0 · 可交付（对应软件实施工程师，岗位量最大，优先做）

1. 四个子服务全部补 Dockerfile，统一进 `infra/docker-compose.yml`，提供 `.env.example`
2. **一键部署脚本**：Linux（bash + systemd 服务单元）+ Windows（PowerShell），支持 `--env dev|test|prod` 切换
3. 多环境配置外置，禁止环境相关值写死在代码里
4. 各服务 `/health` 健康检查端点；统一日志格式（时间 / 级别 / trace_id / 模块）+ 日志切割
5. 备份恢复脚本：MySQL + PostgreSQL 全量/增量备份与恢复，附演练记录
6. 编写**部署手册 + 运维手册**（安装前置、端口表、常见故障处理、升级步骤）
7. MCP 工具名对齐并**真正挂载**
8. Kafka 死信可观测 + 可重放
9. `/api/chat/stream` 接入鉴权与限流
10. `tts-service` 英文标识符中文化收尾

**判据**：干净的 Linux 机上一条命令起全栈；`--env prod` 可切换；全服务 `/health` 可达；备份脚本恢复演练成功；两个手册可照着独立完成部署；死信可在 `/api/v1/sync/status` 查到并重放；三个端点均需凭证；全仓无英文业务标识符。

### P1 · 可测试（对应软件测试岗位）

1. **pytest**：ai-service + tts-service，覆盖异步路径，接入 `pytest-cov`
2. **Spring Boot Test + Testcontainers**：backend-java（不用裸 JUnit+Mockito，避免与 XRM 重复）
3. **E2E**：Playwright 覆盖登录 / 问答 / 编排三条主流程
4. **测试用例文档**（中文）：用例编号 + 前置条件 + 步骤 + 预期结果，≥50 条
5. **CI 门禁**：GitHub Actions，单测 + 覆盖率阈值 + E2E，PR 必绿

**判据**：后端覆盖率 ≥70%；E2E 三条主流程通过；用例文档 ≥50 条且可追溯到实现；CI 全绿。

### P2 · 数据与报表（对应 Java 开发 / BI 报表 / 数据开发）

1. **复杂 SQL 能力**：多表关联、窗口函数、分页优化；每处附优化前后 `EXPLAIN` 对比
2. **报表导出**：Excel（openpyxl）+ PDF（reportlab），支持按模板定时生成与下载
3. **数仓分层**：ODS → DWD → DWS，库表命名与文档真实体现
4. **数据质量校验**：每批次行数 / 空值率 / 主键唯一性 / 波动阈值，超限告警并落质量表
5. 调度可观测：任务依赖图、执行时长、成功率、迟到数据标记，前端「运营看板」展示

**判据**：导出的 Excel/PDF 文件真实可打开且数据正确；质量校验超限能告警并落表；至少有 3 处 SQL 优化附 `EXPLAIN` 前后对比；看板展示依赖图与成功率。

### P3 · 工具总线与开放集成（AI 岗 + 集成能力）

1. `ToolRegistry` 唯一真源：工具定义一次（pydantic schema + 执行函数），**五处导出** —— ① LangGraph `bind_tools` ② MCP Server(Streamable HTTP) ③ 编排器节点类型 ④ OpenAPI 文档 ⑤ n8n 自定义节点描述
2. 开放 API：`/api/v1/**`，API Key 鉴权（哈希存储）+ 按 Key 限流配额 + 自动 OpenAPI 3.1 文档
3. Webhook 出站：事件（流程完成/审批待办/同步失败）→ HMAC 签名 → 重试队列 → 投递记录可查
4. Webhook 入站：外部触发流程，签名校验 + 幂等键
5. 渠道适配器：统一抽象 `通知渠道` 接口，实现飞书 / 企微 / 钉钉 / SMTP 邮件。**未配置则自动跳过，不得阻断主流程**

**判据**：工具定义仅一份无重复；MCP 客户端能列出并调用全部工具；Webhook 投递记录可查且失败自动重试；缺渠道配置时流程不失败；工具调用测试覆盖 100%。

### P4 · 可视化编排（AI 岗 + 低代码实施岗）

1. **画布**：`@xyflow/react`。节点 8 类：开始 / LLM / 工具调用 / 条件分支 / 循环 / 代码 / 人工审批 / 结束
2. **流程 DSL**：画布 ⇄ YAML 双向转换，落 Postgres，**带版本号**（可回滚可 diff）
3. **DAG 执行引擎**（Python）：解析 DSL → 建图 → 拓扑排序 → 调度；节点处理器注册表**复用 `ToolRegistry`**（禁止第二套）；每节点支持超时、指数退避重试、错误分支、节点级 trace span、输入输出快照；无依赖节点并行 + 人工审批节点挂起/回调恢复
4. **执行回放**：每节点高亮状态（成功/失败/跳过/等待中）
5. **n8n**：容器入 compose；TypeScript 自定义节点 ≥4 个（发起问答 / 触发流程 / 查询统计 / TTS）；可运行示例工作流 ≥2 条

**判据**：画布上从零搭出 ≥5 节点流程并**真实跑通**；DSL 导入导出幂等；审批节点可挂起并由回调恢复；引擎单测覆盖环形检测与超时分支；n8n 中能看到并调用本项目节点，示例工作流端到端跑通。

### P5 · Agent 内核与检索（AI 岗深度）

1. LangGraph 五节点 `Supervisor → Planner → Executor → Reflector → Synthesizer`；Checkpoint 持久化（重启可恢复）+ HITL 中断/恢复 + 轨迹事件
2. 三级记忆：working（内存有界窗口）/ episodic（Postgres，**异步后台压缩，绝不阻塞主链路**）/ semantic（pgvector 检索召回，带置信度与过期）
3. Model Gateway：Provider 抽象 + 能力标签路由 + 健康检查 + 熔断 + 降级链（主模型 → 备选 → 语义缓存 → 兜底模板）+ token 与金额预算
4. **前置**：Postgres 镜像换成 `pgvector/pgvector:pg18`（官方 `postgres:16` **不含 vector 扩展**，不换 `CREATE EXTENSION vector` 必失败）。Chroma → pgvector，检索链路：向量(HNSW) + 全文(`tsvector`/BM25) → RRF 融合 → CrossEncoder 精排（`bge-reranker-v2-m3`，CPU 可跑）→ Top-K；**强制引用溯源**
5. **自研轻量 Trace**（**不用 OTel 全家桶**，见 4.1）：Span 模型落 Postgres，任一请求可按 `trace_id` 回放
6. 评测集 YAML（问题 + 期望工具 + 期望检索 doc + 期望答案要点）；指标 = 工具选择准确率 / Recall@K / MRR / 答案忠实度 / P95 时延 / 单次成本；**CI 回归门禁**
7. 护栏中间件：注入检测、PII 脱敏、输出过滤

**判据**：Recall@5 较 Chroma 基线提升且有数字；答案必带引用；评测集 ≥30 例在 CI 运行且指标无劣化；重启后会话可恢复；主模型故障自动降级且有测试覆盖。

### P6 · 文档与前端（售前/文档岗 + 前端岗）

1. **文档体系**：架构文档 · API 文档 · 部署手册 · 运维手册 · 用户手册 · FAQ · **故障排查手册**（现象 → 原因 → 命令 → 解决）
2. 前端对齐：React 19.2.8 + Vite 8.2.2 + TS 7.0.2 + ECharts 6.1.0（仅为版本对齐，**非技术亮点**，不得写进技术栈清单）
3. backend-java 升 JDK 25；**Spring Boot 3.5.16 → 4.1.1 + Spring AI 2.0.1**（3.5.x / 1.x 已 EOL；2.0 有破坏性变更，按官方迁移指南逐项过）
4. 可选：uni-app 小程序端（版本开工时查，社区版迭代快）

**判据**：新人照文档可独立完成部署与排障；E2E 覆盖三条主流程；Web Vitals 达标（LCP ≤2.5s / INP ≤200ms / CLS ≤0.1）；全仓无 EOL 主版本。

> 时间有限时，最低交付为 **P0 + P1 + P2**（这三项对应岗位量最大的实施 / 测试 / Java / BI 方向）。

### 不要做

部署 Dify 全集（13 容器过重）· Kubernetes · Milvus/Qdrant 独立向量服务 · vLLM/Ollama 本地推理 · SFT/LoRA/量化 · GraphRAG/知识图谱 · Ragas/Langfuse 重依赖 · 微服务拆分 · 自研画布渲染（直接用 React Flow）· 再搭 OTel + Prometheus + Grafana（见 4.1）

---

## 6. 版本基线

### 6.1 本地环境（2026-09-05 实测，项目版本以此为准）

| 项 | 本地版本 |
| --- | --- |
| Node.js | **22.22.2** |
| npm | **10.9.7** |
| Java | **25.0.4**（Oracle GraalVM 25.2.4） |
| Python | **3.14.7**（另有 3.13.14；项目 venv 现为 3.12.13，**P0 需重建**） |
| MySQL | **26.7.0** |
| Maven | **4.0.0-rc-6** |
| Docker | **29.7.2** |
| Git | **2.55.0** |

### 6.2 容器镜像（本地无，取官方最新 GA）

| 中间件 | 版本 |
| --- | --- |
| PostgreSQL | **18.6** |
| pgvector | **0.8.6**（镜像 `pgvector/pgvector:pg18`） |
| Redis | **8.10.1** |
| Apache Kafka | **4.3.1** |
| n8n | **2.37.10** |

### 6.3 依赖（2026-09-05 实测；每期开工前重查官方源，有更新以更新者为准）

**Python**：FastAPI 0.141.1 · uvicorn 0.52.4 · LangGraph 1.2.11 · langchain-core 1.6.2 · **mcp 2.1.1** · **fastmcp 4.0.3** · pydantic 2.13.5 · pydantic-settings 2.15.0 · sentence-transformers 6.0.1 · httpx 0.28.1 · sqlalchemy 2.0.52 · alembic 1.19.2 · **pytest 9.1.1** · pytest-asyncio 1.4.0 · pytest-cov 7.1.0 · openpyxl 3.1.5 · reportlab 5.0.1 · edge-tts 7.2.8 · langgraph-checkpoint 4.2.0 · langsmith 0.12.1

**Java**：Spring Boot **4.1.1** · Spring AI **2.0.1** · MyBatis-Plus 3.5.17 · JDK 25

**前端**：React 19.2.8 · Vite 8.2.2 · TypeScript **7.0.2** · ECharts **6.1.0** · **@xyflow/react 12.11.6**（MIT）· Vitest 5.0.0 · @playwright/test 1.63.0

**禁止引入任何已 EOL 主版本**（Spring Boot 3.x、MySQL 8.0、Python 3.12 及以下、React 18 主线）。

---

## 7. 编码规范（强制，违反即返工）

1. **中文标识符**：全项目已是中文命名（类/方法/变量），保持一致，禁止新引入英文或拼音。P0 收尾 `tts-service` 残留英文。
2. **零硬编码**：模型名、阈值、超参、超时、端口、环境、渠道配置一律进 `pydantic-settings` / `application.yml`，支持多环境切换与热重载。
3. **凭据安全**：数据库密码、API Key、渠道密钥一律环境变量，禁止入库、禁止硬编码。
4. **用户可见文本走文案模块**（`ai-service/app/texts.py` 等），错误消息与日志同样适用。
5. **输入验证白名单**：音色、模型名、时间范围、ID、Webhook 来源、渠道类型等外部参数一律白名单校验，禁止直接透传下游。
6. **异常处理**：非法数据不得崩溃；统一错误码与文案，错误消息不暴露内部实现；**渠道适配器与外部平台调用失败一律降级跳过，不得阻断主流程**。
7. **测试**：先写测试再实现；改完跑全量；禁止为通过而改测试；P1 起 CI 强制门禁。
8. **不重复实现**：工具、Trace、鉴权、限流等横切能力各只一份；新增能力一律复用 `ToolRegistry` 与中间件链。
9. **技术栈纪律**：严格遵守第 4 节边界——属于 4.1 清单的技术只做实现，不写进 README 技术栈、不作亮点包装。
10. **发现计划存在技术矛盾或不可行 → 停下来报告**，不要自行改动架构方向。

---

## 8. 验收清单

- [ ] P0–P6 全部完成，每项有对应测试
- [ ] **可交付**：干净机器一条命令起全栈；部署手册 + 运维手册 + 故障排查手册齐备
- [ ] **可测试**：后端覆盖率 ≥70%；E2E 覆盖三条主流程；中文测试用例文档 ≥50 条
- [ ] **可观测（自研 Trace）**：任一请求可按 `trace_id` 回放完整轨迹；**未引入 OTel/Prometheus/Grafana 全家桶**
- [ ] 画布上可搭 ≥5 节点流程并真实跑通，执行可回放；n8n 自定义节点可用，示例工作流 ≥2 条跑通
- [ ] 工具定义仅一份，五处导出一致；MCP 客户端可列出并调用全部工具
- [ ] 报表导出（Excel/PDF）真实可打开；数据质量校验超限告警并落表；≥3 处 SQL 优化附 `EXPLAIN` 对比
- [ ] 评测集 ≥30 例在 CI 运行，指标无劣化
- [ ] 性能预算达标且有实测数字（非估算）：首 token P95 ≤800ms（缓存命中 ≤150ms）· 单轮成本 ≤0.01 元 · 常驻内存 ≤400MB · 检索 P95 ≤250ms · 编排器节点调度开销 ≤20ms/节点 · 画布 100 节点 LCP ≤2.5s · Web Vitals：LCP ≤2.5s / INP ≤200ms / CLS ≤0.1
- [ ] 全仓依赖对齐第 6 节基线，无 EOL 主版本；Python venv ≥3.14
- [ ] 无死代码、无重复实现、无"写了但没接线"、无硬编码凭据
- [ ] README 重写：定位为「可交付、可运维、可测试的 AI 数据平台」，含架构图 + 一键部署步骤 + 完整文档索引；**技术栈清单只列第 4.2 节独有技术**

---

## 9. 开工提示

1. 先 `docker compose -f infra/docker-compose.yml up -d` 跑通基线，确认能起再动手。
2. **P0 优先且不可跳过**——它对应岗位量最大的实施/运维方向，且阻塞后续所有工作。
3. P3 的 `ToolRegistry` 是架构支点，P4/P5 建立在它之上，优先做对。
4. P4 工作量最大：**先做执行引擎（可脱离 UI 单测），后做画布**。
5. 每完成一期，更新本文件勾选状态，并在 README/文档目录补充该期产出。
6. 动手前先读第 4 节技术栈边界，别把其他项目已有的技术又包装成亮点。
