# 恋爱吧数据中心 · 优化计划

> 目标读者：接手本文件、即将开始写代码的 AI Agent。
> 本文件是经过市场调研与可行性裁决后的**最终结论**，不含调研过程。直接按第 5 节分期开工。

---

## 0. 一句话定位

把项目从「AI 问答 + 数据大屏」升级为 **`LianAI Agent Runtime`——生产级 LLM Agent 中台**：
一个具备**工具总线 / 记忆 / 检索 / 模型治理 / 可观测 / 评测**六大能力平面的 Agent 运行内核；数据大屏退化为它的一个消费方。

**对 HR 的叙事**：不是"我调过 DeepSeek API"，而是"我能独立设计一个可观测、可评测、可降级、可人工介入的生产级 Agent 系统"。

---

## 1. 为什么要改：统一根因

现状暴露的问题不是各自独立的 bug，而是**同一个根因的不同症状**：

| 症状 | 表面原因 | 真实根因 |
| --- | --- | --- |
| MCP 服务器写了却没挂载；MCP 工具名与 Agent 工具名不一致 | 忘了接线 | 工具以**函数形式硬编码**在 `agent/tools.py`，没有统一注册中心，同一能力被迫在两处各写一遍 |
| Kafka 死信 topic 无人消费 | 少写个消费者 | 数据管道**没有统一的失败处理与补偿组件** |
| 无记忆；历史靠前端回传 10 轮 | 没做 | 会话状态只活在请求体里，服务端**无会话存储层** |
| 无可观测、无评测 | 没做 | Agent 执行是**黑盒函数调用**，没有 trace/span 抽象可供埋点 |
| 无护栏；`/api/chat/stream` 裸奔且能经工具回查内部统计 | 没做 | 缺少**统一入口与中间件链**，横切能力无处挂载 |
| ~~tts 死代码（同步预查、兼容旧错误别名）~~ **本期已清理** | 忘了删 | 能力演进后**旧接口未清理**，无清理机制 |
| tts 服务部分文件（cache/provider/voices）从未提交 git，导致远端仓库 router 引用不存在的模块 | 漏提交 | 缺少"提交前验证仓库可独立克隆构建"的门禁 |

**统一根因**：缺少「Agent Runtime」这一执行内核层，导致**横切能力（工具、记忆、观测、评测、安全、模型治理）无处挂载**，只能以临时代码零散实现或干脆缺失。

**统一解法**：引入 Agent Runtime 作为唯一执行内核，横切能力做成标准中间件/插件挂载点。**一次解决所有同类问题**，后续新增能力不再需要改执行主链路。

---

## 2. 现状盘点

### 2.1 已有（可复用，不要重写）

| 能力 | 现状 | 位置 |
| --- | --- | --- |
| LangGraph 图编排 | 4 节点 plan→execute→reflect→generate | `ai-service/app/agent/graph.py` |
| Function Calling | 6 个工具 | `ai-service/app/agent/tools.py` |
| RAG | Chroma + 本地 SentenceTransformer | `ai-service/app/rag/` |
| SSE 流式 | 自写 SSE + 前端 ReadableStream 解析 | `ai-service/app/api/`、`前端 utils/sse-parser.ts` |
| 数据平面 | Postgres 源库 → Kafka ETL → MySQL 快照 → Redis → Spring Boot API | 全项目 |
| 缓存 | Redis single-flight + 空标记 + 抖动 TTL | `backend-java/cache/` |
| TTS | edge-tts 主路 + 离线正弦兜底 + LRU 单飞 | `tts-service/` |
| 前端大屏 | React 18 + ECharts | `analytics-frontend/` |

> 上表描述的是**现状**（含旧版本）。目标版本一律以 **6.3 版本基线**为准，升级动作已排入 P3/P6，不得因现状旧而沿用旧版。

### 2.2 缺口（按市场需求强度排序）

| 缺口 | 市场需求 | 现状 |
| --- | --- | --- |
| MCP 真正打通 | 高（JD 硬指标） | 有代码未接线，工具名不一致 |
| 记忆（短期/长期） | 高 | 无 |
| Human-in-the-loop | 高 | 无 |
| 混合检索 + Rerank | 高 | 仅向量，无 BM25 无精排 |
| Agent 可观测（trace） | 高 | 无 |
| Agent 评测（evals） | 高 | 无 |
| Guardrails 护栏 | 高 | 无 |
| 模型路由/降级/成本 | 高 | 单模型，无路由无预算 |
| 多智能体协作 | 高 | 仅单 Agent |
| 持久化 Checkpoint | 高 | 无，会话不可恢复 |
| Spring AI（Java 侧 Agent） | 中高 | 无 |
| CI/CD | 高 | 无 |

---

## 3. 市场调研结论（2026-09 实抓）

**可投岗位（本科应届可达，按需求量排序）**

| 排序 | 岗位 | 应届可及性 | 参考薪资 |
| --- | --- | --- | --- |
| 1 | 大模型应用开发工程师 | 本科可投 | 年包 25–40 万 |
| 2 | AI 应用开发工程师（LLM/RAG） | 本科明确可投 | 10–15K/月，年包 18–30 万 |
| 3 | AI Agent 开发工程师 | 校招本科岗存在但偏少 | 15–20K/月 |
| 4 | RAG 工程师 | 多并入上述岗位 | — |
| — | 大模型算法 / AI Infra | **硕士起，本科慎投** | — |

**技术关键词频次**（真实 JD 样本统计）：Python · RAG · Prompt工程 · Function/Tool Calling · LangChain/LangGraph · 记忆 · 多智能体 · 向量数据库 · MCP · ReAct/Plan。

**结论**：岗位名称不统一但内核一致，均为"RAG + 工具调用 + 编排 + 可观测 + 评测 + 工程化"。**补上第 2.2 节缺口 = 直接命中 JD 关键词**。

> **数据来源与效力说明**（面试被问到要能讲清楚）：以上结论抓取自 2026-09 的公开 JD，样本来源含 国家大学生就业服务平台(ncss.cn)、南开大学就业网、厦门人才网(xmrc.com.cn)、全职招聘网(quanzhi.com)、应届生求职网、智联校园、全职招聘、以及 GitHub 官方仓库(langchain-ai/langgraph、modelcontextprotocol/servers)。**薪资为公开报道/爆料区间估算，非官方定薪，误差可达 ±20%，面试时按"公开渠道参考值"表述，不要说成自己的 offer 水平。** BOSS直聘/猎聘/牛客需登录，仅取搜索摘要，未做逐页统计。

---

## 4. 目标架构

```
                  ┌──────────────────────────────────┐
   前端/第三方 ──▶│  Gateway（FastAPI 单一入口）      │
                  │  鉴权 · 限流 · 配额 · 会话路由     │
                  └───────────────┬──────────────────┘
                                  │
                  ┌───────────────▼──────────────────┐
                  │  Middleware Chain（可插拔中间件）  │
                  │  护栏入 → 追踪 → 缓存 → 护栏出     │
                  │  → 成本计量                        │
                  └───────────────┬──────────────────┘
                                  │
                  ┌───────────────▼──────────────────┐
                  │  Agent Runtime（LangGraph）        │
                  │  Supervisor → Planner → Executor  │
                  │  → Reflector → Synthesizer        │
                  │  Checkpoint 持久化 · HITL 中断恢复 │
                  └───────────────┬──────────────────┘
                                  │
      ┌───────────┬───────────────┼───────────────┬──────────────┐
      ▼           ▼               ▼               ▼              ▼
 ┌─────────┐ ┌──────────┐  ┌───────────┐  ┌────────────┐
 │ToolPlane│ │RAG Engine│  │  Memory   │  │ModelGateway│
 │MCP 总线 │ │混合检索  │  │ 三级记忆  │  │路由/降级   │
 │跨语言   │ │+Rerank   │  │异步摘要   │  │预算/限流   │
 └────┬────┘ └────┬─────┘  └─────┬─────┘  └──────┬─────┘
      │           │              │               │
      └───────────┴──────────────┴───────────────┘
                          │
      ┌───────────────────▼──────────────────────┐
      │  治理平面                                 │
      │  Observability(Trace/回放) + Evaluation   │
      └───────────────────┬──────────────────────┘
                          │
      ┌───────────────────▼──────────────────────┐
      │  数据平面（已有）                         │
      │  Postgres源 → Kafka → MySQL → Redis       │
      └──────────────────────────────────────────┘
```

### 4.1 Tool Plane（MCP 工具总线）—— 根除"工具定义两处"

- **唯一真源**：每个工具定义**一次**（pydantic schema + 执行函数），注册进 `ToolRegistry`。
- **一份注册，三处导出**：① LangGraph `bind_tools` schema ② MCP 服务器（Streamable HTTP）③ OpenAPI 文档。
- **跨语言复用**：Java 侧以 Spring AI MCP Client 消费同一批工具 —— **Python 与 Java 共享同一工具总线**，这是最强展示点，也直接消灭"两处不一致"的根因。
- 危险工具打标 `requires_approval` → 触发 HITL。

### 4.2 RAG Engine —— 混合检索 + 精排

- 存储**替换为 pgvector**（复用 Postgres，不新增独立中间件）。Chroma 无 BM25、无 rerank、多一个运维组件，淘汰。
- ⚠️ **落地前置条件**：官方 `postgres:16` 镜像**不含 vector 扩展**。必须把 `infra/docker-compose.yml` 里的镜像换成 `pgvector/pgvector:pg18`（pgvector 0.8.6，对应 PostgreSQL 18），否则 `CREATE EXTENSION vector` 会失败。这一步在 P3 开工前必须先做。
- 检索链路：向量（pgvector HNSW）+ 全文（Postgres `tsvector`/BM25）→ **RRF 融合** → CrossEncoder 精排（`bge-reranker-v2-m3`，CPU 可跑）→ Top-K。
- 分块改为**标题感知 + 滑动窗口重叠**，替代固定 500 字切分。
- **强制引用溯源**：每片段带 `doc_id`/`chunk_id`，答案必须带引用，可点开原文。

### 4.3 Memory —— 三级记忆

| 层级 | 内容 | 存储 | 约束 |
| --- | --- | --- | --- |
| working | 会话窗口内原始消息 | 内存 | 有界窗口 |
| episodic | 会话摘要 | Postgres | **异步后台压缩，绝不阻塞主链路** |
| semantic | 长期事实/用户画像 | pgvector | 检索式召回，带置信度与过期 |

### 4.4 Model Gateway —— 路由 / 降级 / 预算

- Provider 抽象（DeepSeek / OpenAI 兼容 / 本地）。**能力标签路由**：chat / vision / reasoning / embedding / rerank。
- 健康检查 + 熔断 + 降级链：主模型 → 备选 → **语义缓存**（embedding 相似命中直接返回）→ 兜底模板。
- 预算：按会话/租户的 token 与金额预算，超限拒绝并降级。

### 4.5 Observability —— 可观测

- 自研 Trace/Span 模型（`trace_id` → span 树），落 Postgres。
- 记录：每步 prompt/completion/token/耗时/工具调用/检索命中/成本。
- OTel 导出（可选接 Langfuse / Phoenix）。
- **轨迹回放**：依托 Checkpoint 重放任意一步，用于复现 bad case。

### 4.6 Evaluation —— 评测与回归门禁

- 评测集 YAML：问题 + 期望工具 + 期望检索 doc + 期望答案要点。
- 指标：工具选择准确率、检索 Recall@K / MRR、答案忠实度（LLM-as-judge）、P95 时延、单次成本。
- **CI 回归门禁**：指标劣化超阈值即 fail，禁止"改完不知道有没有变差"。

---

## 5. 分期实施路线（每期独立可验证，按序推进）

| 期 | 内容 | 完成判据（可机器验证） |
| --- | --- | --- |
| **P0** | 收口与护栏：MCP 工具名对齐并真正挂载；`/api/chat/stream` 接入鉴权与限流；Kafka 死信可观测+可重放；标识符中文化收尾（`tts-service` 的 `provider.py`/`voices.py` 仍残留 `code/message/retryable/rate/pitch` 等英文）；加 CI 门禁验证"克隆后能独立构建" | 三个端点均需凭证；死信可在 `/api/v1/sync/status` 查到并重放；全仓无英文业务标识符；全量测试通过 |
| **P1** | Tool Plane：`ToolRegistry` 唯一真源 + MCP Server(Streamable HTTP) + 三处导出 | 工具定义仅一份；MCP 客户端能列出并调用全部工具；工具调用测试覆盖 100% |
| **P2** | 执行内核：LangGraph 五节点 + Checkpoint 持久化 + HITL 中断/恢复 + 轨迹事件 | 进程重启后会话可恢复；危险工具触发中断且可 resume；轨迹事件完整 |
| **P3** | 检索升级：pgvector + tsvector 混合检索 + RRF + Rerank + 引用溯源 | Recall@5 较 Chroma 基线提升且有数字；答案必带引用；检索 P95 达标 |
| **P4** | 三级记忆 + 异步摘要 | 跨会话事实可召回；摘要不增加首 token 时延（有前后对比数字） |
| **P5** | 治理：Trace/回放 + 评测集 + CI 门禁 + 输入/输出护栏 | 任一请求可查完整 trace；评测集 ≥30 例并在 CI 运行；护栏拦截率有测试覆盖 |
| **P6** | Model Gateway + 多智能体 + Java Spring AI 消费 MCP + 前端控制台 + CI/CD；**Spring Boot 3.5.16→4.1.1 + Spring AI 2.0.1 整体升级**；compose 镜像升级（MySQL 8.4 LTS / Redis 8 / Kafka 4.3） | 主模型故障自动降级且测试覆盖；Java 侧可调同一批工具；全仓依赖为最新 GA（无 EOL 版本）；GitHub Actions 全绿 |

> **P0–P2 是核心价值**，若时间有限，至少完成 P0–P2 + P5。

---

## 6. 技术选型裁决

### 6.1 采纳

| 技术 | 理由 |
| --- | --- |
| LangGraph | JD 高频；原生 Checkpoint + HITL，正是我们要的执行内核 |
| MCP（Streamable HTTP） | JD 硬指标；根治工具双写；可跨语言 |
| pgvector | 复用现有 Postgres，**不新增中间件**；同时支持向量 + 全文，混合检索一步到位 |
| CrossEncoder Rerank | JD 高频"Rerank"；本地 CPU 可跑，零成本 |
| Guardrails + HITL | 生产级 Agent 必选项，JD 高频 |
| 自建 Trace + 评测 | 比接 Ragas/Langfuse 更轻量且更能展示自研能力；避免重度依赖 |
| Spring AI（Java 侧） | 展示 Java 栈 Agent 能力；**直接用最新 GA：Spring AI 2.0.1 + Spring Boot 4.1.1**（Spring AI 2.0 需配 Boot 4，1.x 已随 Boot 3.5 停更，禁止从 1.x 起步） |
| GitHub Actions | CI/CD 是 JD 高频工程化要求 |

> ⚠️ **版本时效风险**：Spring Boot 3.5 与 Spring AI 1.x 已于 2026-06 EOL。现状代码仍跑在 3.5.16 上，**P6 必须整体升级到 Spring Boot 4.1.1 + Spring AI 2.0.1**（Spring Framework 7 / JDK 25）；Spring AI 2.0 存在 API 破坏性变更，升级时按官方迁移指南逐项过。禁止新代码继续建立在 EOL 版本上。

### 6.3 版本基线（强制，2026-09-04 联网查证；实现时以当日最新为准重查一遍）

**所有依赖一律锁定当时最新稳定 GA 版本**，禁止沿用旧版本"图省事"。下表是开工时的最新基线（来源：PyPI / npm / Maven Central / endoflife.date / 官方 Releases，均实际抓取核验）：

| 技术 | 最新稳定 GA | 现状 | 动作 |
| --- | --- | --- | --- |
| Python | 3.14.7（3.13 系补丁 3.13.15） | 3.13 venv | 重建 venv 至 3.14 |
| FastAPI | 0.141.1 | 0.141.1 / 0.115.0(tts) | tts 升级对齐 |
| uvicorn | 0.52.4 | 0.32.0(tts) | 升级 |
| LangGraph | 1.2.11 | 1.2.11 | 保持 |
| langchain-core | 1.6.2 | 1.6.0 | 升级 |
| chromadb | 1.5.9 | 1.5.9 | P3 起淘汰，迁 pgvector |
| pgvector | 0.8.6（镜像 `pgvector/pgvector:pg18`） | 无 | P3 引入 |
| pydantic / pydantic-settings | 2.13.5 / 2.15.0 | 2.15.0 / 2.15.0 | pydantic 升级 |
| Spring Boot | **4.1.1**（3.5.16 已 EOL） | 3.5.16 | P6 升级 |
| Spring AI | **2.0.1** GA | 无 | P6 引入 |
| JDK | 25（最新 LTS，Temurin 25.0.4.1+1） | 25 | 保持 |
| MyBatis-Plus | 3.5.17 | 3.5.17 | 保持 |
| Apache Kafka | 4.3.1 | 3.8.0 | compose 镜像升级 |
| Redis | 8.10.1 | 7-alpine | compose 镜像升级 |
| PostgreSQL | 18.4 | 16 | P3 随 pgvector 镜像升级 |
| MySQL | 8.4 LTS（8.4.11；8.0 已 EOL） | 8.0 | compose 镜像升级至 8.4 |
| Node.js | 24 LTS（24.19.0） | 26.7.0 运行可用 | 保持，CI 锚定 24 LTS |
| Vite / React | 8.2.2 / 19.2.8 | 8.2.x / 19.x | 补丁对齐 |
| sentence-transformers | 6.0.1 | — | P3 引入 |
| Rerank 模型 | `bge-reranker-v2-m3`（仍是轻量多语金标准） | 无 | P3 引入 |

> **执行规则**：每期开工前先重查上表对应项的最新版（官方源），再写入依赖清单；若查到更新版本，以更新者为准并在提交信息中注明。面试被问"为什么是这个版本"，回答口径是"锁定最新稳定 GA 并跟踪官方 EOL 公告"。

### 6.2 不采纳（明确拒绝，避免返工）

| 技术 | 拒绝理由 |
| --- | --- |
| Milvus / Qdrant / 独立向量服务 | 需新增中间件；pgvector 已满足且运维成本为零 |
| Kubernetes | 过重；docker-compose 足够，K8s 对展示无增量 |
| vLLM / Ollama / 本地推理 | 需 GPU，本机无卡；且偏离"应用层"定位 |
| SFT / LoRA / 量化 | 需 GPU + 数据集；算法岗才需要，本科应用岗不需 |
| GraphRAG / 知识图谱 | 复杂度高、收益不确定；列为可选扩展，不进主线 |
| Ragas / Langfuse 等重依赖 | 自建更轻，且自主实现更能体现能力（保留 OTel 导出兼容） |
| Chroma（现状） | P3 起淘汰，迁移到 pgvector |

---

## 7. 性能预算（硬指标，必须量化验证）

| 指标 | 目标 |
| --- | --- |
| P95 首 token 时延（未命中缓存） | ≤ 800 ms |
| P95 首 token 时延（语义缓存命中） | ≤ 150 ms |
| 单轮问答成本 | ≤ 0.01 元；语义缓存命中率 ≥ 25% |
| 常驻内存（不含模型） | ≤ 400 MB |
| 检索 P95（混合检索 + Rerank） | ≤ 250 ms |
| 冷启动 | 模型/embedding 单例预加载，首个请求不额外惩罚 |

**压缩手段（强制）**：全链路异步无阻塞；embedding 批量化；HNSW `ef_search` 调优；流式 token 零拷贝直出（禁止全量缓冲）；连接池复用；Checkpoint 懒加载；摘要/评测/追踪全部旁路异步。

---

## 8. 编码规范（强制，违反即返工）

1. **中文标识符**：全项目已是中文命名（类/方法/变量），**保持一致**，禁止新引入英文/拼音标识符。
2. **零硬编码**：模型名、阈值、超参、超时、端口一律进配置（`pydantic-settings` / `application.yml`），支持热重载。
3. **用户可见文本走文案模块**（`ai-service/app/texts.py` 等），禁止散落硬编码；错误消息与日志同样适用。
4. **输入验证白名单**：所有外部参数（音色、模型名、时间范围、ID）白名单校验，禁止直接透传给下游。
5. **异常处理**：非法数据不得导致服务崩溃；统一错误码与文案，错误消息不暴露内部实现。
6. **测试**：每完成一个能力点，先写测试再实现；改完跑全量测试，禁止为通过而改测试。
7. **不自动同意**：若实现中发现本计划存在技术矛盾或不可行，**停下来报告**，不要自行改动架构方向。
8. **依赖版本强制最新构建版**：所有新增依赖一律锁定当日最新稳定 GA（基线见 6.3 节）；每期开工前重查一遍官方源（PyPI/npm/Maven Central/endoflife.date），发现更新版本以更新者为准；**禁止引入任何已 EOL 的主版本**（如 Spring Boot 3.x、MySQL 8.0、Python 3.12 及以下）。

---

## 9. 验收标准（整体完成判据）

- [ ] 六个能力平面全部落地，且每个平面有对应测试
- [ ] 全仓依赖版本等于 6.3 节基线或更新（无 EOL 主版本，`mvn versions:display-dependency-updates` / `pip list --outdated` / `npm outdated` 无主版本落后）
- [ ] 第 7 节性能预算全部达标，并有实测数字（不是估算）
- [ ] 评测集 ≥ 30 例在 CI 中运行，指标无劣化
- [ ] 全量测试通过，无编译错误，无 lint 报错
- [ ] 任一请求可通过 trace_id 回放完整轨迹
- [ ] README 更新为"Agent 中台"叙事，含架构图与本地启动步骤
- [ ] 无死代码、无重复实现、无"写了但没接线"的功能

---

## 10. 给下一个 Agent 的开工提示

1. **先跑通现状**：`docker compose -f infra/docker-compose.yml up -d`，然后启动 backend-java / ai-service / frontend，确认基线可运行，再动手。
2. **从 P0 开始**，P0 未完成的收口项（MCP 挂载、流式端点鉴权、死信重放）会阻塞后续所有工作。
3. **P1 是架构支点**：`ToolRegistry` 一旦建立，P2/P3/P6 都建立在它之上，优先做对。
4. 每完成一期，更新本文件的对应勾选状态，并在 README 补充该期的能力说明。
