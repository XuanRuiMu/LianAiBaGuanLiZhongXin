import { 应用标题, 加载中, 暂无数据, 待补充, 未收录, 查询, 重置, 上一页, 下一页, 返回, 提交, 详情, 操作, 未记录, 无适用值, 全部, 已隐藏, 确认, 取消, 二次确认 } from '../术语/通用';
import { 条目单位 } from '../术语/公共';

type 通用键清单 = {
  应用标题: string;
  加载中: string;
  暂无数据: string;
  待补充: string;
  查询: string;
  重置: string;
  上一页: string;
  下一页: string;
  返回: string;
  提交: string;
  请求失败: string;
  登录过期: string;
  网络中断: string;
  请求超时: string;
  响应格式异常: string;
  操作未执行: string;
  数据未更新: string;
  重新登录后重试: string;
  切换权限账号: string;
  修正后重试: string;
  确认后重试: string;
  刷新后重试: string;
  稍后重试: string;
  等待后重试: string;
  可重试: string;
  不可重试: string;
  重试当前操作: string;
  错误码: string;
  错误影响: string;
  错误下一步: string;
  错误追踪编号: string;
  错误可重试: string;
  字段问题: string;
  总数前缀: string;
  总数后缀: string;
  详情: string;
  操作列: string;
  未收录: string;
  未记录: string;
  无适用值: string;
  全部: string;
  已隐藏: string;
  确认: string;
  取消: string;
  二次确认: string;
};

export const 通用文案 = {
  应用标题: 应用标题,
  加载中: 加载中,
  暂无数据: 暂无数据,
  待补充: 待补充,
  查询: 查询,
  重置: 重置,
  上一页: 上一页,
  下一页: 下一页,
  返回: 返回,
  提交: 提交,
  请求失败: '请求失败，请稍后再试',
  登录过期: '登录已过期，请重新登录',
  网络中断: '网络连接已中断，请检查连接后重试',
  请求超时: '请求等待超时，请稍后重试',
  响应格式异常: '服务响应格式异常，请稍后重试',
  操作未执行: '当前操作未执行，现有数据未改变',
  数据未更新: '本次操作未完成，页面数据未更新',
  重新登录后重试: '请重新登录后重试当前操作',
  切换权限账号: '请切换有权限的管理账号后重试',
  修正后重试: '请按提示修正后重新提交',
  确认后重试: '请确认后重试当前操作',
  刷新后重试: '请刷新数据并核对最新状态后重试',
  稍后重试: '请稍后重试；持续失败请提供追踪编号',
  等待后重试: '请等待 {秒数} 秒后重试当前操作',
  可重试: '可重试',
  不可重试: '不可重试',
  重试当前操作: '重试当前操作',
  错误码: '错误码',
  错误影响: '影响',
  错误下一步: '下一步',
  错误追踪编号: '追踪编号',
  错误可重试: '可重试性',
  字段问题: '字段问题',
  总数前缀: '共',
  总数后缀: 条目单位,
  详情: 详情,
  操作列: 操作,
  未收录: 未收录,
  未记录: 未记录,
  无适用值: 无适用值,
  全部: 全部,
  已隐藏: 已隐藏,
  确认: 确认,
  取消: 取消,
  二次确认: 二次确认,
} as const satisfies 通用键清单;
