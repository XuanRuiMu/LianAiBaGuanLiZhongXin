import { 待初审, 待复审, 已通过, 已关闭, 已发布, 已下线, 进行中, 已结束, 运行中, 优先级低, 优先级中, 优先级高, 优先级紧急, 审验无, 通过, 驳回 } from '../术语/审核';
import { 已驳回 } from '../术语/公共';

export type 审核枚举键清单 = {
  状态待初审: string;
  状态待复审: string;
  状态已通过: string;
  状态已驳回: string;
  状态已关闭: string;
  状态已发布: string;
  状态已下线: string;
  状态进行中: string;
  状态已结束: string;
  状态运行中: string;
  优先级低: string;
  优先级中: string;
  优先级高: string;
  优先级紧急: string;
  结论无: string;
  结论通过: string;
  结论驳回: string;
};

export const 审核枚举文案 = {
  状态待初审: 待初审,
  状态待复审: 待复审,
  状态已通过: 已通过,
  状态已驳回: 已驳回,
  状态已关闭: 已关闭,
  状态已发布: 已发布,
  状态已下线: 已下线,
  状态进行中: 进行中,
  状态已结束: 已结束,
  状态运行中: 运行中,
  优先级低: 优先级低,
  优先级中: 优先级中,
  优先级高: 优先级高,
  优先级紧急: 优先级紧急,
  结论无: 审验无,
  结论通过: 通过,
  结论驳回: 驳回,
} as const satisfies 审核枚举键清单;
