# 遗留问题（给下一个 AI Agent）

> 状态基线：P0–P4、P6 已做完并验证；P5 除检索数字外完成。未推送 GitHub（用户自行推送）。
> 进度与证据见 `PROGRESS.md`，待验证清单见《优化计划.md》§5/§8（标"待验证"处）。

## E1 模型权重下载通道不可达（阻塞检索数字，最高优）

- 现象：`paraphrase-multilingual-MiniLM-L12-v2` 续传 3 次约 85 分钟，
  镜像站大文件停滞 0B/s，官方源约 145KB/s，`.incomplete` 停在 256MB。
  `bge-reranker-v2-m3` 更大，未开始。
- 缓存位置：`C:\Users\xuanr\.cache\huggingface\hub\`（续传有效，别删 `.incomplete`）。
- 网络恢复后依次跑：`app/记忆/入库.py`（知识入库 PG）→
  `pytest tests/test_评测.py`（检索 10 例自动解除跳过）→ 回填 Recall@5/MRR 数字。
- 代码侧已就绪：混合检索 RRF、CrossEncoder 精排（降级链完整）、评测框架、CI 任务。

## E2 全栈 docker 构建未跑通

- `ai-service` 镜像构建因 pip 超时中断一次（torch 大包）；Dockerfile 已加
  `PIP_INDEX_URL`/`PIP_TRUSTED_HOST` 构建参数，`pywin32` 已拆到 `requirements-windows.txt`。
- 后端/前端镜像构建命令未执行过；`backend-java`/`analytics-frontend` 的
  Dockerfile 构建阶段中文名已改 ASCII（旧 Docker 报 invalid name）。
- 验证：在网络好时跑 `docker compose build` 全量＋`deploy.ps1 -env dev`。

## E3 CI 从未在 GitHub 跑过

- `.github/workflows/ci.yml` 四任务已写（含 pgvector 服务＋评测），本地全绿，
  但无一次真实 CI 运行。推送后看首次运行结果修。

## E4 需真机/真密钥的手工项

- 真实渠道发送（飞书/企微/钉钉/SMTP）：代码＋缺配跳过已验证，真实投递需密钥。
- 新人照文档独立部署＋排障：文档齐，无人走过。
- Web Vitals 实测数字：无（需 Lighthouse/真实浏览器采集）。
- ai 本地 venv 仍是 Python 3.12（重装 torch 链同 E1 网络原因；Dockerfile 已切 3.14）。

## E5 已知坑（别重复踩）

- 后台进程跨轮次会被回收：`java/python/vite/Docker Desktop` 掉了就重拉，
  用根目录 `本地启动.ps1 -env dev`（JWT/INTERNAL_TOKEN 口径已统一）。
- `docker exec` 传中文 SQL 必坏：写文件＋`docker cp`＋`psql -f`。
- PowerShell 管道传中文必坏：同上，禁 `Get-Content ... | docker exec -i`。
- Write 工具偶发写 BOM：Java/SQL 文件写后必查 `\ufeff`。
- `n8n execute` CLI 不加载自定义扩展；生产 webhook 需激活＋重启仍可能未注册；
  节点逻辑用直驱脚本验证（见 PROGRESS 决策记录）。
- HTTP 头名/查询参数名必须 ASCII（中文头名直接 500，中文参数名 curl 报非法请求）。
