# 面试答辩文档 —— 恋爱吧数据中心

> 用途：面试前复习。每个技术亮点 = 选型理由 + 高频追问预演。
> 数据口径：演示环境 120 用户 / 200 角色 / 8000 消息（程序生成的演示数据，已如实标注）。

---

## 一、60 秒项目介绍（背诵版）

「和我恋爱吧」是我独立开发的 AI 恋爱聊天应用，已经上线。随着数据变多，我为它自建了一个运营数据平台：用 Spring Boot 3 做数据中台，通过 Kafka 把 PostgreSQL 业务库的统计聚合异步同步到 MySQL 分析库，Redis 做了缓存穿透/击穿/雪崩的三重防护；AI 侧用 Python FastAPI + LangGraph 编排 ReAct Agent，接了 RAG 知识库，并把能力封装成 MCP Server；前端是 React + ECharts 的运营大屏和流式对话。三端一共 80 多个自动化测试。

## 二、技术亮点与追问预演

### 1. 为什么用 Kafka？（必问）

**答**：同步链路是「定时任务发指令 → Kafka → 消费者拉源库聚合并写分析库」。选 Kafka 的理由：
1. **解耦**：源库不可用时，指令在 topic 里堆积，恢复后自动补跑，调度器不阻塞；
2. **削峰**：手动触发和定时触发可能撞车，队列天然串行化；
3. **可观测**：每个消息有 offset，失败进 DLT（死信队列）可追溯可回放。

**追问：为什么不用 Spring 自带的 @Scheduled 直接跑？**
→ 可以，但那样失败重试、执行历史、手动/自动触发的统一入口都要自己造；Kafka 把这些变成基础设施能力，且这是校招 JD 里出现率最高的中间件，值得引入。（诚实版答案）

**追问：消息重复消费怎么办？**
→ 幂等设计：消费者按 `batchId + statType` 查 sync_log，已有 SUCCESS 记录直接跳过；写入侧 UPSERT（ON DUPLICATE KEY UPDATE）保证结果幂等。

**追问：怎么保证不丢消息？**
→ 生产者 acks=all；消费侧先处理成功再手动提交位移（如配置为 manual ack），失败重试 2 次后进 DLT 并记录 sync_log，可人工补偿。

### 2. Redis 缓存三防（必问）

| 问题 | 方案 | 实现 |
|---|---|---|
| 穿透（查不存在的数据打穿到库） | 空值缓存 | 未命中结果缓存 60s 空标记 |
| 击穿（热点 key 过期瞬间打垮库） | 互斥锁重建 | SETNX 抢锁(30s 过期+finally Lua 释放)，未抢到的短暂等待后二查，仍无则降级直查 |
| 雪崩（大量 key 同时过期） | TTL 随机化 | 1800s + Random(0,300s) |

**追问：锁的过期时间和业务时间不一致怎么办？**
→ 锁值存唯一标识，释放时用 Lua 脚本校验"是自己持有的锁才删"，防止误删他人锁；极端情况锁到期业务未完，会有短暂双查，但查询是只读的，无害。

**追问：为什么空标记 60 秒？**
→ 平衡攻击成本与数据新鲜度：统计类数据分钟级更新，60s 内恶意重复查询同一不存在资源全部被挡在 Redis。

### 3. MySQL 索引优化

- 统计表全部按 `stat_date` 建唯一键，天然支持 UPSERT 与范围扫描
- `sync_message_daily` 上建复合索引 `idx_date_usercnt(stat_date, user_msg_count)` 用于覆盖索引场景
- 附 Explain 对比实验方法：灌入 50 万行压测数据后对比全表扫描 vs 索引扫描的 rows/extra 差异（实验脚本见答辩备注）

**追问：为什么统计表不用宽表/列式存储？**
→ 当前量级（万级行）MySQL 足够；如果日增千万事件，正确演进是 Kafka → ClickHouse/Doris，这正是我把 Kafka 放在链路中间的原因——存储层可替换。

### 4. LangGraph ReAct Agent

**架构**：StateGraph 三节点循环——plan（拆解问题）→ tool_loop（LLM 决定调哪个工具，最多 6 轮迭代保护）→ generate（汇总生成）；工具结果不足时反思节点会换检索词重试一次。

**追问：Agent 怎么决定调用哪个工具？**
→ 每个工具有严格的 docstring + 参数 schema（Pydantic 校验），LangChain 把它们转成 function calling 的 tool 定义，模型按描述匹配意图。

**追问：怎么防提示注入？**
→ 工具层只接受结构化白名单参数（days 限制 7~30 等），SQL 全部参数化预编译，模型输出永远不会拼接进 SQL；内部接口走独立 X-Internal-Token，与用户 JWT 隔离。

**追问：RAG 的检索流程？**
→ knowledge/*.md → 500 字切块（overlap 50）→ 多语言 MiniLM 本地向量化（384 维）→ ChromaDB 持久化 → top_k=4 余弦检索 → 拼 Prompt。选本地 embedding 是因为 DeepSeek 无 embedding 接口，且本地化零 API 成本。

### 5. MCP Server 有什么用？

把「运营数据查询」封装成标准 MCP tools（query_overview / query_user_trend / query_favorability / search_knowledge），任何 MCP 客户端（Claude、Cursor 等）都能即插即用这份数据能力——这就是 AI Agent 时代的"API 开放平台"。stdio 模式仅本机可用，无网络暴露面。

### 6. 前端 SSE 流式

**追问：为什么不用 EventSource？**
→ EventSource 只支持 GET 且不能带自定义 Authorization 头；我用 fetch + ReadableStream 手动解析 SSE 帧（data: 行按空行分帧、跨 chunk 断句重组），解析器是纯函数，8 个单测覆盖边界（多行 data、残包、[DONE]）。

### 7. 双数据源事务边界

分析库（MySQL 写）与源库（PG 只读）分池管理，源连接 readOnly=true 从驱动层禁止写。跨库无法用一个事务，靠 Kafka 重试 + 幂等 UPSERT 达到最终一致。

## 三、数字备查

| 项 | 值 |
|---|---|
| Java 测试 | 31 个（缓存三防/JWT/过滤器/消费者幂等/统计组装） |
| Python 测试 | 26 个（工具校验/respx mock/graph 编译/SSE 格式） |
| 前端测试 | 23 个（SSE 解析器 8 / 格式化 11 / i18n 4） |
| 容器数 | 4（MySQL/Redis/Kafka/PostgreSQL） |
| 同步类型 | 6 类统计 × 日快照 |

## 四、诚实话术（被戳破时用）

- 数据是程序生成的演示数据（README 已声明），但表结构与线上产品完全一致；接生产库只改连接串。
- AI 回答依赖 DeepSeek API，断网时 Agent 会优雅返回错误事件而不是崩溃。
