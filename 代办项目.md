# 代办项目 —— 「和我恋爱吧 × 恋爱吧数据中心」功能扩展路线图

> 本文档由 2026-08-26 会话产出并经用户裁决修订。所有条目均源自真实产品缺口，非凭空造需求。
> 范围限定：仅扩展「和我恋爱吧」与「恋爱吧数据中心」；不触碰 Minecraft 插件 XRM 及任何游戏服。
> 已裁决剔除：角色闲置召回（暂不处理）、恋爱关系周报（与产品主题不符）。
> 执行者须知：动手前先读工作区根 `AGENTS.md` 与对应子项目 `AGENTS.md`；每项完成后跑全量测试再交付。

---

## P1 AI 角色语音回复 TTS

### 需求背景（真实场景）
用户可以上传语音消息（`config\媒体配置.ts` 允许 audio/*），但对应用户的 AI 角色**只会打字**——恋爱吧 backend 全部源码无任何 tts 代码。陪伴类产品中语音是核心体验诉求，交互不对等。

### 是否需要"语音大模型"？（选型结论）

不需要单独搞"语音大模型"，直接用**TTS（语音合成）服务**即可，分三条路线：

| 路线 | 代表方案 | 成本 | 优劣 |
|---|---|---|---|
| **A. MiniMax TTS API（推荐）** | 用户已有 MiniMax 账号和 key（音乐开发目录在用），平台同账号开通 speech-2.5-hd 等语音合成模型即可 | 按字计费，新用户有免费额度，量大后便宜 | 中文自然度高、**支持多音色+音色克隆**（不同角色配不同声线，契合产品）；接入方式与现有 DeepSeek/MiniMax music 调用同构，AI AGENT 可直接复刻 generate_music.py 的调用模式 |
| B. edge-tts 免费 | 微软 Edge 朗读接口的 Python 库，无需 key | 完全免费 | 音色固定为微软腔（晓晓/云希等）、无音色克隆；非官方接口存在失效风险，只适合兜底演示 |
| C. 开源模型本地部署 | CosyVoice2（阿里）/ GPT-SoVITS / ChatTTS | 免费但要 GPU | 本地部署+推理对机器要求高（CPU 极慢），运维复杂度与本项目的轻量架构不符，**不推荐本轮做** |

**决策建议**：主路线 A（MiniMax，凭据现成、多音色契合角色扮演），B 作为降级兜底开关（供应商可配置化设计天然支持）。实施 AGENT 动手前先向用户确认 MiniMax 语音合成的额度与计费。

### 方案概要
1. 新增独立 Python 微服务 `tts-service`（FastAPI，端口 8001）：封装 MiniMax TTS 调用（供应商可配置，预留 edge-tts 免费兜底实现），文本+音色参数 → mp3 字节流；
2. 角色卡新增「音色」字段映射表（如温柔女声/成熟男声等预设清单，写入角色配置而非硬编码）；
3. 恋爱吧 backend 在 AI 回复生成后异步调用 tts-service，音频落现有 SHA256 CAS 媒体存储，消息表新增语音消息类型（复用 mediaId 字段）；
4. 恋爱吧前端气泡增加播放按钮；TTS 失败静默降级为纯文字（不得阻塞文字回复主链路）；
5. 开关配置：角色粒度的"语音回复概率"，避免每条都转语音造成费用失控。

### 新增技术栈（简历关键词）
音频流处理、第三方 API 容错集成、异步管道、降级设计、多供应商适配

### 涉及模块
新建 `tts-service`（放工作区或恋爱吧 monorepo 内由实施时定）、`和我恋爱吧/backend`、恋爱吧 Vue 前端

### 验收标准
- 发一条消息收到文字回复后 3s 内出现同内容的语音气泡且可播放
- 不同角色使用各自配置的音色
- 断开 tts-service 时文字回复不受影响
- pytest 覆盖 tts-service 的文本校验、超时与供应商切换降级

---

## P2 留存分析看板

### 需求背景（真实场景）
次日/7 日留存率是聊天应用最核心的运营指标，恋爱吧管理端目前只有总量计数和 AI 用量看板，数据中心也没有留存维度。作为"运营数据分析平台"，缺留存等于缺灵魂。

### 方案概要
1. backend-java 源库新增留存聚合 SQL：以注册日为锚，计算各批次用户的次日/3日/7日回访率（口径：注册后第 N 天当日有过消息行为即算回访，依据「消息.用户ID + 创建时间」）；
2. 分析库新增 `stat_retention_trend` 表（stat_date, cohort_size, day1_rate, day3_rate, day7_rate），纳入 Kafka 同步体系（新同步类型 RETENTION）；
3. 对外 API `/api/v1/stats/retention?days=30`（Redis 缓存三防照常套用）；
4. 大屏新增留存曲线卡片（ECharts 折线，三条率值轴）；
5. ai-service 新增工具 query_retention(days) 供 Agent 解答留存问题，MCP 同步暴露。

### 新增技术栈（简历关键词）
留存口径设计与 SQL 聚合深化、同期群(Cohort)分析

### 涉及模块
`恋爱吧数据中心/backend-java`（Mapper/API/schema）、`analytics-frontend`（图表卡）、`ai-service`（工具+RAG 补充留存口径说明）

### 验收标准
- API 返回近 30 天每日的 1/3/7 日留存率，数值可用 SQL 手工核对一致
- vitest 新增留存图表数据组装用例；JUnit 新增聚合映射测试
- AI 助手提问"最近7天留存怎么样"能调用工具并正确解读

---

## 总览

| 编号 | 名称 | 主要新增关键词 | 风险 | 依赖 |
|---|---|---|---|---|
| P1 | 角色语音回复 TTS | 音频流/多供应商适配/降级 | 低 | MiniMax 额度确认 |
| P2 | 留存分析看板 | 同期群/留存口径 | 低 | 无 |

> 简历叙事总纲：聊天应用产生真实业务数据 → 数据中台聚合分析 → AI 助手解读洞察 → 体验升级（角色开口说话）。双栈工程 + 数据闭环 + 多媒体处理，覆盖面完整。
